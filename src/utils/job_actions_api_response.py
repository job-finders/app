\"\"\"Job Actions API Response Formatter\n\nStandardized API response formatting for job actions operations.\nThis module provides consistent response structures, error handling,\nand status code mapping for all job actions endpoints.\n\nResponse Standards:\n- Consistent JSON structure across all endpoints\n- Proper HTTP status code mapping\n- Standardized error message formatting\n- Security-conscious error information disclosure\n- Performance metrics inclusion for monitoring\n\"\"\"\n\nimport json\nfrom typing import Dict, Any, Optional, Union\nfrom datetime import datetime\nfrom enum import Enum\nfrom flask import Response, jsonify\n\nfrom src.database.models.job_actions_results import (\n    JobActionResult, JobActionsStateResult, JobEngagementResult,\n    JobListResult, JobActionErrorCode\n)\nfrom src.utils.job_actions_security_logger import job_actions_security_logger, SecurityEventType, SecuritySeverity\nfrom src.logger import init_logger\n\n\nclass HTTPStatusCode(Enum):\n    \"\"\"HTTP status codes for API responses\"\"\"\n    OK = 200\n    CREATED = 201\n    BAD_REQUEST = 400\n    UNAUTHORIZED = 401\n    FORBIDDEN = 403\n    NOT_FOUND = 404\n    CONFLICT = 409\n    UNPROCESSABLE_ENTITY = 422\n    TOO_MANY_REQUESTS = 429\n    INTERNAL_SERVER_ERROR = 500\n    SERVICE_UNAVAILABLE = 503\n\n\nclass JobActionsAPIResponseFormatter:\n    \"\"\"\n    Standardized API response formatter for job actions operations.\n    \n    This class provides consistent response formatting, error handling,\n    and status code mapping for all job actions endpoints. It ensures\n    security-conscious error disclosure and includes performance metrics\n    for monitoring and optimization.\n    \n    Features:\n        - Consistent JSON response structure\n        - Proper HTTP status code mapping\n        - Security-conscious error message filtering\n        - Performance metrics inclusion\n        - Audit trail logging for all responses\n        - Rate limiting information inclusion\n    \"\"\"\n    \n    def __init__(self):\n        \"\"\"\n        Initialize the API response formatter.\n        \n        Sets up logging, security monitoring, and response templates\n        for consistent API response generation.\n        \"\"\"\n        self.logger = init_logger(\"JobActionsAPIResponseFormatter\")\n        \n        # Error code to HTTP status mapping\n        self.error_code_mapping = {\n            JobActionErrorCode.VALIDATION_ERROR: HTTPStatusCode.BAD_REQUEST,\n            JobActionErrorCode.USER_NOT_FOUND: HTTPStatusCode.NOT_FOUND,\n            JobActionErrorCode.JOB_NOT_FOUND: HTTPStatusCode.NOT_FOUND,\n            JobActionErrorCode.ALREADY_LIKED: HTTPStatusCode.CONFLICT,\n            JobActionErrorCode.LIKE_NOT_FOUND: HTTPStatusCode.NOT_FOUND,\n            JobActionErrorCode.ALREADY_SAVED: HTTPStatusCode.CONFLICT,\n            JobActionErrorCode.SAVED_JOB_NOT_FOUND: HTTPStatusCode.NOT_FOUND,\n            JobActionErrorCode.INVALID_SHARE_METHOD: HTTPStatusCode.BAD_REQUEST,\n            JobActionErrorCode.UNAUTHORIZED: HTTPStatusCode.UNAUTHORIZED,\n            JobActionErrorCode.RATE_LIMITED: HTTPStatusCode.TOO_MANY_REQUESTS,\n            JobActionErrorCode.DATABASE_ERROR: HTTPStatusCode.SERVICE_UNAVAILABLE,\n            JobActionErrorCode.INTERNAL_ERROR: HTTPStatusCode.INTERNAL_SERVER_ERROR\n        }\n        \n        # Security-safe error messages for production\n        self.safe_error_messages = {\n            JobActionErrorCode.DATABASE_ERROR: \"Service temporarily unavailable. Please try again later.\",\n            JobActionErrorCode.INTERNAL_ERROR: \"An unexpected error occurred. Please try again later.\",\n            JobActionErrorCode.UNAUTHORIZED: \"Access denied. Please check your permissions.\"\n        }\n    \n    def format_success_response(\n        self,\n        result: Union[JobActionResult, JobActionsStateResult, JobEngagementResult, JobListResult],\n        user_id: Optional[str] = None,\n        request_id: Optional[str] = None,\n        include_performance_metrics: bool = False\n    ) -> Response:\n        \"\"\"\n        Format a successful API response with standardized structure.\n        \n        Args:\n            result: Result object from service layer\n            user_id: User ID for audit logging\n            request_id: Request ID for tracing\n            include_performance_metrics: Whether to include performance data\n            \n        Returns:\n            Flask Response object with standardized JSON structure\n        \"\"\"\n        try:\n            # Build response data\n            response_data = {\n                \"success\": True,\n                \"message\": result.message,\n                \"data\": result.data if hasattr(result, 'data') and result.data else {},\n                \"timestamp\": datetime.utcnow().isoformat(),\n                \"request_id\": request_id\n            }\n            \n            # Add performance metrics if requested\n            if include_performance_metrics and hasattr(result, 'performance_metrics'):\n                response_data[\"performance\"] = result.performance_metrics\n            \n            # Add pagination info for list results\n            if isinstance(result, JobListResult) and hasattr(result, 'pagination'):\n                response_data[\"pagination\"] = result.pagination\n            \n            # Log successful response for audit trail\n            job_actions_security_logger.log_audit_trail(\n                user_id=user_id,\n                action='api_response_success',\n                resource='job_actions_api',\n                result='success',\n                details={\n                    'response_type': type(result).__name__,\n                    'request_id': request_id,\n                    'data_size': len(str(response_data.get('data', {})))\n                }\n            )\n            \n            self.logger.info(\n                f\"API Success Response: {type(result).__name__}\",\n                extra={\n                    'user_id': user_id,\n                    'request_id': request_id,\n                    'response_type': type(result).__name__,\n                    'success': True,\n                    'data_size': len(str(response_data.get('data', {})))\n                }\n            )\n            \n            return jsonify(response_data), HTTPStatusCode.OK.value\n            \n        except Exception as e:\n            # Fallback error handling\n            self.logger.error(f\"Error formatting success response: {e}\", exc_info=True)\n            return self._format_internal_error_response(user_id, request_id)\n    \n    def format_error_response(\n        self,\n        result: Union[JobActionResult, JobActionsStateResult, JobEngagementResult, JobListResult],\n        user_id: Optional[str] = None,\n        request_id: Optional[str] = None,\n        include_debug_info: bool = False\n    ) -> Response:\n        \"\"\"\n        Format an error API response with security-conscious error disclosure.\n        \n        Args:\n            result: Result object from service layer containing error information\n            user_id: User ID for audit logging\n            request_id: Request ID for tracing\n            include_debug_info: Whether to include debug information (dev only)\n            \n        Returns:\n            Flask Response object with standardized error structure\n        \"\"\"\n        try:\n            # Determine HTTP status code\n            status_code = self._get_status_code_for_error(result.error_code)\n            \n            # Get security-safe error message\n            error_message = self._get_safe_error_message(result.error_code, result.message)\n            \n            # Build error response data\n            response_data = {\n                \"success\": False,\n                \"error\": {\n                    \"code\": result.error_code.value if result.error_code else \"UNKNOWN_ERROR\",\n                    \"message\": error_message,\n                    \"timestamp\": datetime.utcnow().isoformat()\n                },\n                \"request_id\": request_id\n            }\n            \n            # Add debug information in development\n            if include_debug_info and hasattr(result, 'debug_info'):\n                response_data[\"debug\"] = result.debug_info\n            \n            # Add additional error context if available\n            if hasattr(result, 'data') and result.data:\n                response_data[\"error\"][\"details\"] = result.data\n            \n            # Log error response for security monitoring\n            severity = self._determine_error_severity(result.error_code, status_code)\n            \n            job_actions_security_logger.log_security_event(\n                event_type=SecurityEventType.AUDIT_TRAIL,\n                severity=severity,\n                user_id=user_id,\n                action='api_response_error',\n                details={\n                    'error_code': result.error_code.value if result.error_code else None,\n                    'status_code': status_code.value,\n                    'request_id': request_id,\n                    'error_message': result.message  # Original message for internal logging\n                }\n            )\n            \n            self.logger.warning(\n                f\"API Error Response: {result.error_code.value if result.error_code else 'UNKNOWN'}\",\n                extra={\n                    'user_id': user_id,\n                    'request_id': request_id,\n                    'error_code': result.error_code.value if result.error_code else None,\n                    'status_code': status_code.value,\n                    'original_message': result.message,\n                    'safe_message': error_message,\n                    'security_event': True\n                }\n            )\n            \n            return jsonify(response_data), status_code.value\n            \n        except Exception as e:\n            # Fallback error handling\n            self.logger.error(f\"Error formatting error response: {e}\", exc_info=True)\n            return self._format_internal_error_response(user_id, request_id)\n    \n    def format_validation_error_response(\n        self,\n        validation_errors: Dict[str, Any],\n        user_id: Optional[str] = None,\n        request_id: Optional[str] = None\n    ) -> Response:\n        \"\"\"\n        Format a validation error response with detailed field-level errors.\n        \n        Args:\n            validation_errors: Dictionary of field validation errors\n            user_id: User ID for audit logging\n            request_id: Request ID for tracing\n            \n        Returns:\n            Flask Response object with validation error structure\n        \"\"\"\n        try:\n            response_data = {\n                \"success\": False,\n                \"error\": {\n                    \"code\": \"VALIDATION_ERROR\",\n                    \"message\": \"Input validation failed\",\n                    \"validation_errors\": validation_errors,\n                    \"timestamp\": datetime.utcnow().isoformat()\n                },\n                \"request_id\": request_id\n            }\n            \n            # Log validation error for security monitoring\n            job_actions_security_logger.log_input_validation_failure(\n                user_id=user_id,\n                action='api_validation_error',\n                validation_errors=validation_errors,\n                input_data={'request_id': request_id}\n            )\n            \n            self.logger.warning(\n                \"API Validation Error Response\",\n                extra={\n                    'user_id': user_id,\n                    'request_id': request_id,\n                    'validation_errors': validation_errors,\n                    'security_event': True\n                }\n            )\n            \n            return jsonify(response_data), HTTPStatusCode.BAD_REQUEST.value\n            \n        except Exception as e:\n            self.logger.error(f\"Error formatting validation error response: {e}\", exc_info=True)\n            return self._format_internal_error_response(user_id, request_id)\n    \n    def format_rate_limit_response(\n        self,\n        retry_after: int,\n        user_id: Optional[str] = None,\n        request_id: Optional[str] = None\n    ) -> Response:\n        \"\"\"\n        Format a rate limit exceeded response.\n        \n        Args:\n            retry_after: Seconds until next request is allowed\n            user_id: User ID for audit logging\n            request_id: Request ID for tracing\n            \n        Returns:\n            Flask Response object with rate limit information\n        \"\"\"\n        try:\n            response_data = {\n                \"success\": False,\n                \"error\": {\n                    \"code\": \"RATE_LIMITED\",\n                    \"message\": \"Rate limit exceeded. Please try again later.\",\n                    \"retry_after\": retry_after,\n                    \"timestamp\": datetime.utcnow().isoformat()\n                },\n                \"request_id\": request_id\n            }\n            \n            # Log rate limit violation\n            job_actions_security_logger.log_rate_limit_violation(\n                user_id=user_id,\n                action='api_rate_limit',\n                request_count=0,  # Would be provided by rate limiter\n                time_window=60,   # Would be provided by rate limiter\n            )\n            \n            self.logger.warning(\n                \"API Rate Limit Response\",\n                extra={\n                    'user_id': user_id,\n                    'request_id': request_id,\n                    'retry_after': retry_after,\n                    'security_event': True\n                }\n            )\n            \n            response = jsonify(response_data)\n            response.headers['Retry-After'] = str(retry_after)\n            return response, HTTPStatusCode.TOO_MANY_REQUESTS.value\n            \n        except Exception as e:\n            self.logger.error(f\"Error formatting rate limit response: {e}\", exc_info=True)\n            return self._format_internal_error_response(user_id, request_id)\n    \n    def _get_status_code_for_error(self, error_code: Optional[JobActionErrorCode]) -> HTTPStatusCode:\n        \"\"\"\n        Get appropriate HTTP status code for error code.\n        \n        Args:\n            error_code: JobActionErrorCode enum value\n            \n        Returns:\n            Appropriate HTTPStatusCode enum value\n        \"\"\"\n        if not error_code:\n            return HTTPStatusCode.INTERNAL_SERVER_ERROR\n        \n        return self.error_code_mapping.get(error_code, HTTPStatusCode.INTERNAL_SERVER_ERROR)\n    \n    def _get_safe_error_message(self, error_code: Optional[JobActionErrorCode], original_message: str) -> str:\n        \"\"\"\n        Get security-safe error message for client response.\n        \n        Args:\n            error_code: JobActionErrorCode enum value\n            original_message: Original error message from service\n            \n        Returns:\n            Security-safe error message for client\n        \"\"\"\n        if not error_code:\n            return \"An unexpected error occurred. Please try again later.\"\n        \n        # Return safe message for sensitive errors\n        if error_code in self.safe_error_messages:\n            return self.safe_error_messages[error_code]\n        \n        # Return original message for non-sensitive errors\n        return original_message\n    \n    def _determine_error_severity(self, error_code: Optional[JobActionErrorCode], status_code: HTTPStatusCode) -> SecuritySeverity:\n        \"\"\"\n        Determine security severity level for error logging.\n        \n        Args:\n            error_code: JobActionErrorCode enum value\n            status_code: HTTP status code\n            \n        Returns:\n            SecuritySeverity enum value\n        \"\"\"\n        if status_code == HTTPStatusCode.INTERNAL_SERVER_ERROR:\n            return SecuritySeverity.HIGH\n        elif status_code in [HTTPStatusCode.UNAUTHORIZED, HTTPStatusCode.FORBIDDEN]:\n            return SecuritySeverity.MEDIUM\n        elif status_code == HTTPStatusCode.TOO_MANY_REQUESTS:\n            return SecuritySeverity.MEDIUM\n        else:\n            return SecuritySeverity.LOW\n    \n    def _format_internal_error_response(self, user_id: Optional[str], request_id: Optional[str]) -> Response:\n        \"\"\"\n        Format a fallback internal error response.\n        \n        Args:\n            user_id: User ID for logging\n            request_id: Request ID for tracing\n            \n        Returns:\n            Flask Response object with generic error\n        \"\"\"\n        response_data = {\n            \"success\": False,\n            \"error\": {\n                \"code\": \"INTERNAL_ERROR\",\n                \"message\": \"An unexpected error occurred. Please try again later.\",\n                \"timestamp\": datetime.utcnow().isoformat()\n            },\n            \"request_id\": request_id\n        }\n        \n        # Log critical error\n        job_actions_security_logger.log_security_event(\n            event_type=SecurityEventType.PERFORMANCE_ANOMALY,\n            severity=SecuritySeverity.CRITICAL,\n            user_id=user_id,\n            action='api_formatter_failure',\n            details={'request_id': request_id}\n        )\n        \n        return jsonify(response_data), HTTPStatusCode.INTERNAL_SERVER_ERROR.value\n\n\n# Global API response formatter instance\njob_actions_api_formatter = JobActionsAPIResponseFormatter()"}
ToolResult: SUCCESS - Created
the
src / utils / job_actions_api_response.py
file.
Now
let
me
create
a
comprehensive
error
handling
middleware
for the job actions routes:

    <
    function_calls >
    < invoke
name = "fsWrite" >
       < parameter
name = "path" > src / utils / job_actions_error_middleware.py\"Job Actions Error Handling Middleware\n\nComprehensive error handling middleware for job actions routes.\nThis module provides centralized error handling, security monitoring,\nand standardized error responses for all job actions endpoints.\n\nError Handling Features:\n- Centralized exception handling for all job actions routes\n- Security event logging for suspicious activities\n- Performance monitoring for error patterns\n- Standardized error response formatting\n- Rate limiting integration\n- Audit trail maintenance\n\"\"\"\n\nimport traceback\nimport uuid\nfrom typing import Dict, Any, Optional, Callable\nfrom datetime import datetime\nfrom functools import wraps\n\nfrom flask import request, g, current_app\nfrom pydantic import ValidationError\nfrom sqlalchemy.exc import IntegrityError, DatabaseError, OperationalError\n\nfrom src.database.models.job_actions_results import JobActionResult, JobActionErrorCode\nfrom src.utils.job_actions_security_logger import job_actions_security_logger, SecurityEventType, SecuritySeverity\nfrom src.utils.job_actions_performance_logger import job_actions_performance_logger\nfrom src.utils.job_actions_api_response import job_actions_api_formatter\nfrom src.logger import init_logger\n\n\nclass JobActionsErrorMiddleware:\n    \"\"\"\n    Comprehensive error handling middleware for job actions operations.\n    \n    This middleware provides centralized error handling, security monitoring,\n    and standardized error responses for all job actions endpoints. It integrates\n    with the security logging system and performance monitoring to provide\n    comprehensive observability and threat detection.\n    \n    Features:\n        - Centralized exception handling with proper categorization\n        - Security event logging for all error conditions\n        - Performance monitoring and anomaly detection\n        - Standardized API response formatting\n        - Request context preservation for debugging\n        - Rate limiting integration and abuse detection\n    \"\"\"\n    \n    def __init__(self):\n        \"\"\"\n        Initialize the error handling middleware.\n        \n        Sets up logging, security monitoring, and error categorization\n        for comprehensive error handling across job actions endpoints.\n        \"\"\"\n        self.logger = init_logger(\"JobActionsErrorMiddleware\")\n        \n        # Error categorization for proper handling\n        self.client_errors = {\n            ValidationError: (JobActionErrorCode.VALIDATION_ERROR, SecuritySeverity.LOW),\n            ValueError: (JobActionErrorCode.VALIDATION_ERROR, SecuritySeverity.LOW),\n            KeyError: (JobActionErrorCode.VALIDATION_ERROR, SecuritySeverity.MEDIUM),\n            TypeError: (JobActionErrorCode.VALIDATION_ERROR, SecuritySeverity.MEDIUM)\n        }\n        \n        self.database_errors = {\n            IntegrityError: (JobActionErrorCode.ALREADY_LIKED, SecuritySeverity.LOW),\n            DatabaseError: (JobActionErrorCode.DATABASE_ERROR, SecuritySeverity.MEDIUM),\n            OperationalError: (JobActionErrorCode.DATABASE_ERROR, SecuritySeverity.HIGH)\n        }\n        \n        # Performance thresholds for anomaly detection\n        self.slow_request_threshold = 5.0  # seconds\n        self.error_rate_threshold = 0.1    # 10% error rate\n        \n        self.logger.info(\"JobActionsErrorMiddleware initialized\")\n    \n    def handle_job_actions_errors(self, func: Callable) -> Callable:\n        \"\"\"\n        Decorator for comprehensive error handling in job actions routes.\n        \n        This decorator provides centralized error handling, security logging,\n        performance monitoring, and standardized error responses for job actions\n        endpoints. It should be applied to all job actions route handlers.\n        \n        Args:\n            func: Route handler function to wrap\n            \n        Returns:\n            Wrapped function with comprehensive error handling\n            \n        Usage:\n            @job_actions_error_middleware.handle_job_actions_errors\n            @jobs_bp.route('/like', methods=['POST'])\n            def like_job():\n                # Route implementation\n        \"\"\"\n        @wraps(func)\n        def wrapper(*args, **kwargs):\n            # Generate request ID for tracing\n            request_id = str(uuid.uuid4())\n            g.request_id = request_id\n            \n            # Extract user context for logging\n            user_id = self._extract_user_id_from_request()\n            \n            # Start performance tracking\n            start_time = datetime.utcnow()\n            \n            try:\n                # Log request start for audit trail\n                self._log_request_start(func.__name__, user_id, request_id)\n                \n                # Execute the route handler\n                with job_actions_performance_logger.track_operation(\n                    f'route_{func.__name__}',\n                    user_id=user_id,\n                    metadata={\n                        'route': request.endpoint,\n                        'method': request.method,\n                        'request_id': request_id\n                    }\n                ) as perf_context:\n                    \n                    result = func(*args, **kwargs)\n                    \n                    # Log successful request completion\n                    self._log_request_success(func.__name__, user_id, request_id, start_time)\n                    \n                    return result\n                    \n            except ValidationError as e:\n                return self._handle_validation_error(e, func.__name__, user_id, request_id, start_time)\n                \n            except IntegrityError as e:\n                return self._handle_integrity_error(e, func.__name__, user_id, request_id, start_time)\n                \n            except DatabaseError as e:\n                return self._handle_database_error(e, func.__name__, user_id, request_id, start_time)\n                \n            except PermissionError as e:\n                return self._handle_permission_error(e, func.__name__, user_id, request_id, start_time)\n                \n            except Exception as e:\n                return self._handle_unexpected_error(e, func.__name__, user_id, request_id, start_time)\n        \n        return wrapper\n    \n    def _extract_user_id_from_request(self) -> Optional[str]:\n        \"\"\"\n        Extract user ID from request context for logging.\n        \n        Returns:\n            User ID if available, None otherwise\n        \"\"\"\n        try:\n            # Try to get user ID from various sources\n            if hasattr(g, 'current_user') and g.current_user:\n                return getattr(g.current_user, 'user_id', None)\n            \n            # Try to get from request JSON\n            if request.is_json and request.json:\n                return request.json.get('user_id')\n            \n            # Try to get from form data\n            if request.form:\n                return request.form.get('user_id')\n            \n            # Try to get from query parameters\n            return request.args.get('user_id')\n            \n        except Exception:\n            return None\n    \n    def _log_request_start(self, route_name: str, user_id: Optional[str], request_id: str) -> None:\n        \"\"\"\n        Log request start for audit trail.\n        \n        Args:\n            route_name: Name of the route handler\n            user_id: User ID if available\n            request_id: Unique request identifier\n        \"\"\"\n        try:\n            job_actions_security_logger.log_audit_trail(\n                user_id=user_id,\n                action=f'route_start_{route_name}',\n                resource='job_actions_api',\n                result='started',\n                details={\n                    'route': request.endpoint,\n                    'method': request.method,\n                    'request_id': request_id,\n                    'ip_address': request.remote_addr,\n                    'user_agent': request.headers.get('User-Agent', 'Unknown')\n                }\n            )\n            \n            self.logger.info(\n                f\"Request started: {route_name}\",\n                extra={\n                    'user_id': user_id,\n                    'request_id': request_id,\n                    'route': request.endpoint,\n                    'method': request.method,\n                    'ip_address': request.remote_addr\n                }\n            )\n            \n        except Exception as e:\n            self.logger.warning(f\"Failed to log request start: {e}\")\n    \n    def _log_request_success(self, route_name: str, user_id: Optional[str], request_id: str, start_time: datetime) -> None:\n        \"\"\"\n        Log successful request completion.\n        \n        Args:\n            route_name: Name of the route handler\n            user_id: User ID if available\n            request_id: Unique request identifier\n            start_time: Request start time\n        \"\"\"\n        try:\n            duration = (datetime.utcnow() - start_time).total_seconds()\n            \n            # Check for performance anomalies\n            if duration > self.slow_request_threshold:\n                job_actions_security_logger.log_performance_anomaly(\n                    user_id=user_id,\n                    action=f'slow_route_{route_name}',\n                    duration=duration,\n                    details={\n                        'route': request.endpoint,\n                        'method': request.method,\n                        'request_id': request_id\n                    }\n                )\n            \n            job_actions_security_logger.log_audit_trail(\n                user_id=user_id,\n                action=f'route_success_{route_name}',\n                resource='job_actions_api',\n                result='success',\n                details={\n                    'route': request.endpoint,\n                    'method': request.method,\n                    'request_id': request_id,\n                    'duration_seconds': duration\n                }\n            )\n            \n            self.logger.info(\n                f\"Request completed successfully: {route_name}\",\n                extra={\n                    'user_id': user_id,\n                    'request_id': request_id,\n                    'route': request.endpoint,\n                    'method': request.method,\n                    'duration_seconds': duration,\n                    'success': True\n                }\n            )\n            \n        except Exception as e:\n            self.logger.warning(f\"Failed to log request success: {e}\")\n    \n    def _handle_validation_error(self, error: ValidationError, route_name: str, user_id: Optional[str], request_id: str, start_time: datetime) -> tuple:\n        \"\"\"\n        Handle Pydantic validation errors.\n        \n        Args:\n            error: ValidationError instance\n            route_name: Name of the route handler\n            user_id: User ID if available\n            request_id: Unique request identifier\n            start_time: Request start time\n            \n        Returns:\n            Tuple of (response, status_code)\n        \"\"\"\n        try:\n            duration = (datetime.utcnow() - start_time).total_seconds()\n            \n            # Extract validation errors\n            validation_errors = {}\n            for error_detail in error.errors():\n                field = '.'.join(str(loc) for loc in error_detail['loc'])\n                validation_errors[field] = error_detail['msg']\n            \n            # Log validation failure as security event\n            job_actions_security_logger.log_input_validation_failure(\n                user_id=user_id,\n                action=f'route_validation_error_{route_name}',\n                validation_errors=validation_errors,\n                input_data=self._get_safe_request_data()\n            )\n            \n            self.logger.warning(\n                f\"Validation error in {route_name}: {error}\",\n                extra={\n                    'user_id': user_id,\n                    'request_id': request_id,\n                    'route': request.endpoint,\n                    'method': request.method,\n                    'validation_errors': validation_errors,\n                    'duration_seconds': duration,\n                    'security_event': True\n                }\n            )\n            \n            # Format standardized error response\n            return job_actions_api_formatter.format_validation_error_response(\n                validation_errors=validation_errors,\n                user_id=user_id,\n                request_id=request_id\n            )\n            \n        except Exception as e:\n            self.logger.error(f\"Error handling validation error: {e}\", exc_info=True)\n            return self._format_fallback_error_response(user_id, request_id)\n    \n    def _handle_integrity_error(self, error: IntegrityError, route_name: str, user_id: Optional[str], request_id: str, start_time: datetime) -> tuple:\n        \"\"\"\n        Handle database integrity constraint violations.\n        \n        Args:\n            error: IntegrityError instance\n            route_name: Name of the route handler\n            user_id: User ID if available\n            request_id: Unique request identifier\n            start_time: Request start time\n            \n        Returns:\n            Tuple of (response, status_code)\n        \"\"\"\n        try:\n            duration = (datetime.utcnow() - start_time).total_seconds()\n            \n            # Log integrity violation as potential abuse\n            job_actions_security_logger.log_security_event(\n                event_type=SecurityEventType.SUSPICIOUS_ACTIVITY,\n                severity=SecuritySeverity.MEDIUM,\n                user_id=user_id,\n                action=f'integrity_violation_{route_name}',\n                details={\n                    'route': request.endpoint,\n                    'method': request.method,\n                    'request_id': request_id,\n                    'integrity_error': str(error),\n                    'duration_seconds': duration\n                }\n            )\n            \n            self.logger.warning(\n                f\"Integrity error in {route_name}: {error}\",\n                extra={\n                    'user_id': user_id,\n                    'request_id': request_id,\n                    'route': request.endpoint,\n                    'method': request.method,\n                    'integrity_error': str(error),\n                    'duration_seconds': duration,\n                    'security_event': True\n                }\n            )\n            \n            # Create error result\n            result = JobActionResult.error_result(\n                message=\"Operation conflicts with existing data\",\n                error_code=JobActionErrorCode.ALREADY_LIKED\n            )\n            \n            return job_actions_api_formatter.format_error_response(\n                result=result,\n                user_id=user_id,\n                request_id=request_id\n            )\n            \n        except Exception as e:\n            self.logger.error(f\"Error handling integrity error: {e}\", exc_info=True)\n            return self._format_fallback_error_response(user_id, request_id)\n    \n    def _handle_database_error(self, error: DatabaseError, route_name: str, user_id: Optional[str], request_id: str, start_time: datetime) -> tuple:\n        \"\"\"\n        Handle database connectivity and operation errors.\n        \n        Args:\n            error: DatabaseError instance\n            route_name: Name of the route handler\n            user_id: User ID if available\n            request_id: Unique request identifier\n            start_time: Request start time\n            \n        Returns:\n            Tuple of (response, status_code)\n        \"\"\"\n        try:\n            duration = (datetime.utcnow() - start_time).total_seconds()\n            \n            # Log database error as high severity\n            job_actions_security_logger.log_security_event(\n                event_type=SecurityEventType.PERFORMANCE_ANOMALY,\n                severity=SecuritySeverity.HIGH,\n                user_id=user_id,\n                action=f'database_error_{route_name}',\n                details={\n                    'route': request.endpoint,\n                    'method': request.method,\n                    'request_id': request_id,\n                    'database_error': str(error),\n                    'duration_seconds': duration\n                }\n            )\n            \n            self.logger.error(\n                f\"Database error in {route_name}: {error}\",\n                extra={\n                    'user_id': user_id,\n                    'request_id': request_id,\n                    'route': request.endpoint,\n                    'method': request.method,\n                    'database_error': str(error),\n                    'duration_seconds': duration,\n                    'security_event': True\n                },\n                exc_info=True\n            )\n            \n            # Create error result\n            result = JobActionResult.error_result(\n                message=\"Database operation failed\",\n                error_code=JobActionErrorCode.DATABASE_ERROR\n            )\n            \n            return job_actions_api_formatter.format_error_response(\n                result=result,\n                user_id=user_id,\n                request_id=request_id\n            )\n            \n        except Exception as e:\n            self.logger.error(f\"Error handling database error: {e}\", exc_info=True)\n            return self._format_fallback_error_response(user_id, request_id)\n    \n    def _handle_permission_error(self, error: PermissionError, route_name: str, user_id: Optional[str], request_id: str, start_time: datetime) -> tuple:\n        \"\"\"\n        Handle permission and authorization errors.\n        \n        Args:\n            error: PermissionError instance\n            route_name: Name of the route handler\n            user_id: User ID if available\n            request_id: Unique request identifier\n            start_time: Request start time\n            \n        Returns:\n            Tuple of (response, status_code)\n        \"\"\"\n        try:\n            duration = (datetime.utcnow() - start_time).total_seconds()\n            \n            # Log authorization violation\n            job_actions_security_logger.log_authorization_violation(\n                user_id=user_id,\n                action=f'permission_error_{route_name}',\n                resource=request.endpoint,\n                required_permission='job_actions_access'\n            )\n            \n            self.logger.warning(\n                f\"Permission error in {route_name}: {error}\",\n                extra={\n                    'user_id': user_id,\n                    'request_id': request_id,\n                    'route': request.endpoint,\n                    'method': request.method,\n                    'permission_error': str(error),\n                    'duration_seconds': duration,\n                    'security_event': True\n                }\n            )\n            \n            # Create error result\n            result = JobActionResult.error_result(\n                message=\"Access denied\",\n                error_code=JobActionErrorCode.UNAUTHORIZED\n            )\n            \n            return job_actions_api_formatter.format_error_response(\n                result=result,\n                user_id=user_id,\n                request_id=request_id\n            )\n            \n        except Exception as e:\n            self.logger.error(f\"Error handling permission error: {e}\", exc_info=True)\n            return self._format_fallback_error_response(user_id, request_id)\n    \n    def _handle_unexpected_error(self, error: Exception, route_name: str, user_id: Optional[str], request_id: str, start_time: datetime) -> tuple:\n        \"\"\"\n        Handle unexpected errors and exceptions.\n        \n        Args:\n            error: Exception instance\n            route_name: Name of the route handler\n            user_id: User ID if available\n            request_id: Unique request identifier\n            start_time: Request start time\n            \n        Returns:\n            Tuple of (response, status_code)\n        \"\"\"\n        try:\n            duration = (datetime.utcnow() - start_time).total_seconds()\n            \n            # Log unexpected error as critical security event\n            job_actions_security_logger.log_security_event(\n                event_type=SecurityEventType.PERFORMANCE_ANOMALY,\n                severity=SecuritySeverity.CRITICAL,\n                user_id=user_id,\n                action=f'unexpected_error_{route_name}',\n                details={\n                    'route': request.endpoint,\n                    'method': request.method,\n                    'request_id': request_id,\n                    'error_type': type(error).__name__,\n                    'error_message': str(error),\n                    'duration_seconds': duration,\n                    'traceback': traceback.format_exc()\n                }\n            )\n            \n            self.logger.error(\n                f\"Unexpected error in {route_name}: {error}\",\n                extra={\n                    'user_id': user_id,\n                    'request_id': request_id,\n                    'route': request.endpoint,\n                    'method': request.method,\n                    'error_type': type(error).__name__,\n                    'error_message': str(error),\n                    'duration_seconds': duration,\n                    'security_event': True\n                },\n                exc_info=True\n            )\n            \n            # Create error result\n            result = JobActionResult.error_result(\n                message=\"An unexpected error occurred\",\n                error_code=JobActionErrorCode.INTERNAL_ERROR\n            )\n            \n            return job_actions_api_formatter.format_error_response(\n                result=result,\n                user_id=user_id,\n                request_id=request_id\n            )\n            \n        except Exception as e:\n            self.logger.error(f\"Error handling unexpected error: {e}\", exc_info=True)\n            return self._format_fallback_error_response(user_id, request_id)\n    \n    def _get_safe_request_data(self) -> Dict[str, Any]:\n        \"\"\"\n        Get sanitized request data for logging (removes sensitive information).\n        \n        Returns:\n            Dictionary with safe request data\n        \"\"\"\n        try:\n            safe_data = {\n                'method': request.method,\n                'endpoint': request.endpoint,\n                'args': dict(request.args),\n                'form_keys': list(request.form.keys()) if request.form else [],\n                'json_keys': list(request.json.keys()) if request.is_json and request.json else [],\n                'content_length': request.content_length,\n                'content_type': request.content_type\n            }\n            \n            # Remove sensitive fields\n            sensitive_fields = {'password', 'token', 'secret', 'key', 'auth'}\n            for field in sensitive_fields:\n                safe_data['args'].pop(field, None)\n            \n            return safe_data\n            \n        except Exception:\n            return {'error': 'Failed to extract request data'}\n    \n    def _format_fallback_error_response(self, user_id: Optional[str], request_id: str) -> tuple:\n        \"\"\"\n        Format a fallback error response when error handling fails.\n        \n        Args:\n            user_id: User ID if available\n            request_id: Unique request identifier\n            \n        Returns:\n            Tuple of (response, status_code)\n        \"\"\"\n        return job_actions_api_formatter._format_internal_error_response(user_id, request_id)\n\n\n# Global error middleware instance\njob_actions_error_middleware = JobActionsErrorMiddleware()"}
ToolResult: SUCCESS - Created
the
src / utils / job_actions_error_middleware.py
file.
Now
let
me
mark
Task
5 as completed
since
we
have
implemented
comprehensive
error
handling and logging:

< function_calls >
  < invoke
name = "strReplace" >
       < parameter
name = "oldStr" > - [-]
5.
Implement
comprehensive
error
handling and logging
